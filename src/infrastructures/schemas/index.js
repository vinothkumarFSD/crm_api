const contactCollection = require('./contacts.schema');
const userCollection = require('./user.schema');
const dealCollection = require('./deals.schema');
const companiesCollection = require('./companies.schema');
const productsCollection = require('./products.schema');

module.exports = {
  contactCollection,
  userCollection,
  productsCollection,
  companiesCollection,
  dealCollection,
};
